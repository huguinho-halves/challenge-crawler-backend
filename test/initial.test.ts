import {describe, expect, test} from '@jest/globals';

describe("Suit for initial tests", () => {
  test("should be return true", () => {

    const result = true;
    expect(result).toBe(true);
  });
});